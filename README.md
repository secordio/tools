# tools

This repo contains multiple utilities. The CDP-lite prototype lives in `user-explorer/`.

## User Explorer

```bash
cd user-explorer
make install
make build
make serve
```

Open: `http://127.0.0.1:8080/app/index.html`

See `user-explorer/README.md` for full details.
